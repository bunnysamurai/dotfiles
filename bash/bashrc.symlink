# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
  . /etc/bashrc
fi

# Execute fish if we're not invoked as bash -c
if [ -z "$BASH_EXECUTION_STRING" ] && [ -z "$NO_FISH" ]; then
  exec fish
fi




